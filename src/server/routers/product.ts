// src/server/routers/product.tsimport { z } from 'zod';import { createTRPCRouter, publicProcedure, protectedProcedure, adminProcedure } from '@/server/trpc';import { TRPCError } from '@trpc/server';import { uploadImage } from '@/lib/cloudinary';import slugify from 'slugify';import { logActivity } from '@/lib/activity';// M4: Max 2MB per base64 image (base64 has ~33% overhead, so 2MB raw ≈ 2.7MB base64)const MAX_IMAGE_BASE64_BYTES = 2.7 * 1024 * 1024;// ─── Category keyword map for auto-analysis ──────────────────────────────────// Maps keywords in a product name to a category name (case-insensitive)const CATEGORY_KEYWORD_MAP: Record<string, string[]> = {  'Groceries & Essentials':    ['rice', 'dal', 'flour', 'atta', 'oil', 'salt', 'sugar', 'turmeric', 'spice', 'masala', 'pulses', 'lentil', 'wheat', 'cereal', 'grain', 'maida', 'grocery', 'provision'],  'Fruits & Vegetables':   ['tomato', 'potato', 'onion', 'carrot', 'spinach', 'cabbage', 'brinjal', 'capsicum', 'peas', 'beans', 'cucumber', 'gourd', 'bitter', 'drumstick', 'ladies finger', 'okra', 'garlic', 'ginger', 'coriander', 'mint', 'vegetable', 'sabzi', 'greens', 'apple', 'mango', 'banana', 'brand', 'grape', 'papaya', 'watermelon', 'melon', 'pomegranate', 'guava', 'pineapple', 'strawberry', 'lemon', 'kiwi', 'pear', 'fruit'],  'Meat & Fish': ['chicken', 'mutton', 'fish', 'prawn', 'egg', 'meat', 'seafood', 'crab', 'lobster', 'sardine', 'tuna', 'salmon', 'beef', 'pork', 'sausage', 'keema'],  'Bakery & Dairy':        ['milk', 'curd', 'paneer', 'butter', 'ghee', 'cheese', 'cream', 'yogurt', 'lassi', 'buttermilk', 'khoya', 'mawa', 'dairy', 'bread', 'cake', 'biscuit', 'cookie', 'muffin', 'croissant', 'rusk', 'toast', 'pastry', 'brownie', 'donut', 'pav', 'bun', 'pizza'],  'Pharmacy & Wellness': ['medicine', 'tablet', 'capsule', 'syrup', 'bandage', 'vitamin', 'supplement', 'wellness', 'ayurvedic', 'homeopathy', 'pharma', 'shampoo', 'soap', 'face wash', 'toothpaste'],  'Electronics & Gadgets':  ['mobile', 'phone', 'charger', 'cable', 'earphone', 'headphone', 'speaker', 'bulb', 'led', 'fan', 'battery', 'adapter', 'usb', 'laptop', 'tablet', 'remote', 'electronic', 'gadget'],  'Fashion & Lifestyle': ['tshirt', 'shirt', 'pant', 'jeans', 'dress', 'saree', 'kurti', 'shoes', 'fashion', 'watch', 'jewelry', 'bag', 'wallet'],  'Home & Kitchen': ['cooker', 'pan', 'tawa', 'knife', 'vessel', 'cleaner', 'detergent', 'mop', 'broom', 'kitchen', 'home', 'decor'],  'Stationery & Office':   ['pen', 'pencil', 'notebook', 'paper', 'book', 'eraser', 'stapler', 'tape', 'scissors', 'marker', 'highlighter', 'folder', 'stationery'],  'Pet Care': ['dog', 'cat', 'food', 'pet', 'shampoo', 'leash', 'aquarium', 'bird'],  'Toys & Baby Care': ['toy', 'diaper', 'baby', 'doll', 'game', 'pampers', 'wipe'],};function autoDetectCategory(name: string, description: string = ''): string | null {  const text = `${name} ${description}`.toLowerCase();  let bestMatch: { category: string; score: number } | null = null;  for (const [category, keywords] of Object.entries(CATEGORY_KEYWORD_MAP)) {    let score = 0;    for (const kw of keywords) {      if (text.includes(kw)) {        // Exact word boundary match scores higher        score += text.includes(` ${kw} `) || text.startsWith(kw) ? 2 : 1;      }    }    if (score > 0 && (!bestMatch || score > bestMatch.score)) {      bestMatch = { category, score };    }  }  return bestMatch?.category ?? null;}export const productRouter = createTRPCRouter({  list: publicProcedure    .input(      z.object({        limit: z.number().min(1).max(100).nullish(),        cursor: z.string().nullish(),        categorySlug: z.string().optional(),        search: z.string().optional(),        featured: z.boolean().optional(),        sortBy: z.enum(['newest', 'price_asc', 'price_desc', 'rating', 'popular']).optional(),      })    )    .query(async ({ ctx, input }) => {      const limit = input.limit ?? 12;      const { cursor, categorySlug, search, featured, sortBy } = input;      const orderBy = sortBy === 'price_asc'  ? { price: 'asc' as const }                    : sortBy === 'price_desc' ? { price: 'desc' as const }                    : sortBy === 'rating'     ? { rating: 'desc' as const }                    : sortBy === 'popular'    ? { viewCount: 'desc' as const }                    : { createdAt: 'desc' as const };      const items = await ctx.prisma.product.findMany({        take: limit + 1,        where: {          isActive: true,          category: categorySlug ? { slug: categorySlug } : undefined,          isFeatured: featured,          OR: search && search.trim().length > 0            ? [                { AND: search.trim().split(/\s+/).map(w => ({ name: { contains: w, mode: 'insensitive' as const } })) },                { AND: search.trim().split(/\s+/).map(w => ({ description: { contains: w, mode: 'insensitive' as const } })) },                { AND: search.trim().split(/\s+/).map(w => ({ tags: { has: w.toLowerCase() } })) },              ]            : undefined,        },        include: {          category: { select: { name: true, slug: true } },        },        cursor: cursor ? { id: cursor } : undefined,        orderBy,      });      let nextCursor: typeof cursor | undefined = undefined;      if (items.length > limit) {        const nextItem = items.pop();        nextCursor = nextItem!.id;      }      return {        products: items,        nextCursor,      };    }),  // ─── AI-style Search: returns exact + related in one call ─────────────────  smartSearch: publicProcedure    .input(      z.object({        query: z.string().min(1).max(100),        limit: z.number().min(1).max(50).default(20),      })    )    .query(async ({ ctx, input }) => {      const { query, limit } = input;      const q = query.trim();      const baseWhere = {        isActive: true,      };      const words = q.split(/\s+/).filter(w => w.length > 0);      const nameContains = words.map(w => ({ name: { contains: w, mode: 'insensitive' as const } }));      const descContains = words.map(w => ({ description: { contains: w, mode: 'insensitive' as const } }));      const tagsContains = words.map(w => ({ tags: { has: w.toLowerCase() } }));      // 1. Exact name match (all words must be in the name)      const exactByName = await ctx.prisma.product.findMany({        take: limit,        where: {          ...baseWhere,          AND: nameContains.length > 0 ? nameContains : undefined,        },        include: {          category: { select: { name: true, slug: true } },        },        orderBy: { rating: 'desc' },      });      // 2. Description + tags match (exclude already found)      const exactIds = new Set(exactByName.map(p => p.id));      const relatedByDesc = await ctx.prisma.product.findMany({        take: limit,        where: {          ...baseWhere,          id: { notIn: Array.from(exactIds) },          OR: words.length > 0 ? [            { AND: descContains },            { AND: tagsContains },          ] : undefined,        },        include: {          category: { select: { name: true, slug: true } },        },        orderBy: { rating: 'desc' },      });      // 3. Related: same category as top exact result (more discovery)      let categoryRelated: typeof exactByName = [];      if (exactByName.length > 0) {        const topCategoryId = exactByName[0].categoryId;        const relatedIds = new Set([...Array.from(exactIds), ...relatedByDesc.map(p => p.id)]);        categoryRelated = await ctx.prisma.product.findMany({          take: 8,          where: {            ...baseWhere,            categoryId: topCategoryId,            id: { notIn: Array.from(relatedIds) },          },          include: {            category: { select: { name: true, slug: true } },          },          orderBy: { rating: 'desc' },        });      }      // Log search to analytics      ctx.prisma.searchLog.create({        data: {          query: q,          userId: ctx.user?.id,          results: exactByName.length + relatedByDesc.length,        },      }).catch(() => {});      // Real-time activity log for Admin      logActivity({        type: 'SEARCH',        action: 'QUERY',        actorId: ctx.user?.id,        actorName: ctx.user?.name || 'Guest',        message: `Search query: "${q}" (${exactByName.length + relatedByDesc.length} results)`,        metadata: { query: q, results: exactByName.length + relatedByDesc.length },      });      // ─── Intent detection: does this query imply a specific category?      const detectedCatName = autoDetectCategory(q);      let suggestedCategory = null;      if (detectedCatName) {        const cat = await ctx.prisma.category.findFirst({          where: { name: { contains: detectedCatName, mode: 'insensitive' } },          select: { name: true, slug: true }        });        if (cat) suggestedCategory = cat;      }      return {        exact: exactByName,        related: [...relatedByDesc, ...categoryRelated],        totalExact: exactByName.length,        suggestedCategory,      };    }),  // ─── Real-time cart item availability check ────────────────────────────────  checkCartAvailability: publicProcedure    .input(z.array(z.string()))    .query(async ({ ctx, input }) => {      if (input.length === 0) return [];            const products = await ctx.prisma.product.findMany({        where: { id: { in: input } },        select: {          id: true,          isActive: true,          stock: true,        },      });      return products.map(p => ({        id: p.id,        isAvailable: p.isActive && p.stock > 0,        stock: p.stock,      }));    }),  // ─── Typeahead suggestions (fast — name only) ─────────────────────────────  suggest: publicProcedure    .input(      z.object({        query: z.string().min(1).max(60),      })    )    .query(async ({ ctx, input }) => {      const { query } = input;      const q = query.trim();      if (q.length < 2) return [];      const words = q.split(/\s+/).filter(w => w.length > 0);      const nameContains = words.map(w => ({ name: { contains: w, mode: 'insensitive' as const } }));      const results = await ctx.prisma.product.findMany({        take: 6,        where: {          isActive: true,          AND: nameContains.length > 0 ? nameContains : undefined,        },        select: {          id: true,          name: true,          price: true,          images: true,          slug: true,          category: { select: { name: true } },        },        orderBy: { rating: 'desc' },      });      return results;    }),  byId: publicProcedure    .input(z.object({ id: z.string() }))    .query(async ({ ctx, input }) => {      const product = await ctx.prisma.product.findFirst({        where: { id: input.id, isActive: true },        include: {          category: { select: { id: true, name: true, slug: true } },          reviews: {            take: 10,            orderBy: { createdAt: 'desc' },            include: { user: { select: { name: true, avatar: true } } },          },        },      });      if (!product) {        throw new TRPCError({ code: 'NOT_FOUND', message: 'Product not found.' });      }      // Bump view count (fire and forget)      ctx.prisma.product.update({ where: { id: input.id }, data: { viewCount: { increment: 1 } } }).catch(() => {});      return product;    }),  bySlug: publicProcedure    .input(z.object({ slug: z.string() }))    .query(async ({ ctx, input }) => {      const product = await ctx.prisma.product.findFirst({        where: { slug: input.slug, isActive: true },        include: {          category: { select: { id: true, name: true, slug: true } },          reviews: {            take: 10,            orderBy: { createdAt: 'desc' },            include: { user: { select: { name: true, avatar: true } } },          },        },      });      if (!product) {        throw new TRPCError({ code: 'NOT_FOUND', message: 'Product not found.' });      }      ctx.prisma.product.update({ where: { id: product.id }, data: { viewCount: { increment: 1 } } }).catch(() => {});      return product;    }),  // ─── Related products for a given product ────────────────────────────────  related: publicProcedure    .input(z.object({ productId: z.string(), limit: z.number().default(6) }))    .query(async ({ ctx, input }) => {      const product = await ctx.prisma.product.findUnique({        where: { id: input.productId },        select: { categoryId: true },      });      if (!product) return [];      return ctx.prisma.product.findMany({        take: input.limit,        where: {          isActive: true,          id: { not: input.productId },          categoryId: product.categoryId,        },        include: {          category: { select: { name: true, slug: true } },        },        orderBy: { rating: 'desc' },      });    }),  categories: publicProcedure.query(async ({ ctx }) => {    return ctx.prisma.category.findMany({      where: { isActive: true },      select: { id: true, name: true, slug: true, image: true, commissionRate: true },      orderBy: { sortOrder: 'asc' },    });  }),  /** Public: get all categories regardless of active status (for admin) */  listAllCategories: publicProcedure.query(async ({ ctx }) => {    return ctx.prisma.category.findMany({      select: { id: true, name: true, slug: true, image: true, commissionRate: true, isActive: true, sortOrder: true },      orderBy: { name: 'asc' },    });  }),  /** ─── ADMIN: Seed standard categories ───────────────── */  seedCategories: protectedProcedure.mutation(async ({ ctx }) => {    if (ctx.user.role !== 'ADMIN') {      throw new TRPCError({ code: 'UNAUTHORIZED', message: 'Admin only' });    }    const categories = [      { name: '₹1 Store', slug: '1-rupee-store', image: 'https://images.unsplash.com/photo-1580519542036-c47de6196ba5?w=800', sortOrder: 0 },      { name: 'Groceries & Essentials', slug: 'groceries', image: 'https://images.unsplash.com/photo-1542838132-92c53300491e?w=800', sortOrder: 1 },      { name: 'Fruits & Vegetables', slug: 'fruits-veg', image: 'https://images.unsplash.com/photo-1610348725531-843dff563e2c?w=800', sortOrder: 2 },      { name: 'Meat & Fish', slug: 'meat-fish', image: 'https://images.unsplash.com/photo-1607623273573-7034ed8a9abd?w=800', sortOrder: 3 },      { name: 'Bakery & Dairy', slug: 'bakery-dairy', image: 'https://images.unsplash.com/photo-1550583724-125581f77833?w=800', sortOrder: 4 },      { name: 'Pharmacy & Wellness', slug: 'pharmacy', image: 'https://images.unsplash.com/photo-1587854692152-cbe660dbbb88?w=800', sortOrder: 5 },      { name: 'Electronics & Gadgets', slug: 'electronics', image: 'https://images.unsplash.com/photo-1498049794561-7780e7231661?w=800', sortOrder: 6 },      { name: 'Fashion & Lifestyle', slug: 'fashion', image: 'https://images.unsplash.com/photo-1445205170230-053b83016050?w=800', sortOrder: 7 },      { name: 'Home & Kitchen', slug: 'home-kitchen', image: 'https://images.unsplash.com/photo-1556911220-e15224bbaf47?w=800', sortOrder: 8 },      { name: 'Stationery & Office', slug: 'stationery', image: 'https://images.unsplash.com/photo-1456735190827-d1262f71b8a3?w=800', sortOrder: 9 },      { name: 'Pet Care', slug: 'pet-care', image: 'https://images.unsplash.com/photo-1516734212186-a967f81ad0d7?w=800', sortOrder: 10 },      { name: 'Toys & Baby Care', slug: 'toys-baby', image: 'https://images.unsplash.com/photo-1532330393533-443990a51d10?w=800', sortOrder: 11 },    ];    for (const cat of categories) {      await ctx.prisma.category.upsert({        where: { slug: cat.slug },        update: { name: cat.name, image: cat.image, sortOrder: cat.sortOrder },        create: cat,      });    }    return { success: true };  }),  getCities: publicProcedure.query(async ({ ctx }) => {    return [];  }),  // L3: autoAnalyze moved to protectedProcedure — prevents unauthenticated rate-abuse on this DB endpoint  autoAnalyze: protectedProcedure    .input(      z.object({        name: z.string().min(2),        description: z.string().optional(),      })    )    .query(async ({ ctx, input }) => {      const detectedCategoryName = autoDetectCategory(input.name, input.description);      if (!detectedCategoryName) {        return { suggestedCategoryId: null, suggestedCategoryName: null, confidence: 'low' as const, hints: [] };      }      const category = await ctx.prisma.category.findFirst({        where: { name: { contains: detectedCategoryName, mode: 'insensitive' }, isActive: true },        select: { id: true, name: true },      });      // Generate description hints based on category      const descriptionHints: string[] = [];      const catLower = detectedCategoryName.toLowerCase();      if (catLower.includes('vegetable') || catLower.includes('fruit')) {        descriptionHints.push('Mention freshness: "Farm-fresh", "Harvested today"');        descriptionHints.push('Add origin: "Grown in Tamil Nadu", "Local farm"');        descriptionHints.push('Add weight/size info');      } else if (catLower.includes('dairy')) {        descriptionHints.push('Mention source: "From local farm", "A2 milk"');        descriptionHints.push('Add fat percentage if applicable');      } else if (catLower.includes('electronic')) {        descriptionHints.push('Add brand, model, and warranty info');        descriptionHints.push('List key specifications');      } else if (catLower.includes('bakery')) {        descriptionHints.push('Mention ingredients or allergens');        descriptionHints.push('Add shelf life / best before info');      } else {        descriptionHints.push('Describe quality, source, and key features');        descriptionHints.push('Add quantity, weight, or dimensions');      }      return {        suggestedCategoryId: category?.id ?? null,        suggestedCategoryName: category?.name ?? detectedCategoryName,        confidence: category ? 'high' as const : 'medium' as const,        hints: descriptionHints,      };    }),  create: adminProcedure    .input(      z.object({        name: z.string().min(3),        description: z.string().min(10),        price: z.number().positive(),        mrp: z.number().positive(),        stock: z.number().int().min(0),        unit: z.string(),        categoryId: z.string(),        // M4: Validate image size server-side (max 2MB per image, up to 5 images)        images: z.array(          z.string()            .startsWith('data:image/')            .refine((s) => s.length <= MAX_IMAGE_BASE64_BYTES, 'Each image must be under 2MB')        ).min(1).max(5),        isFeatured: z.boolean().optional(),        gstRate: z.number().min(0).max(0.28).default(0),        tags: z.array(z.string()).optional(),        lowStockThreshold: z.number().int().min(1).default(5),      })    )    .mutation(async ({ ctx, input }) => {      const imageUrls = await Promise.all(        input.images.map((base64) => uploadImage(base64, 'products'))      );      const slug = `${slugify(input.name, { lower: true, strict: true })}-${Date.now()}`;      // Auto-generate tags from name if not provided      const autoTags = input.tags?.length        ? input.tags        : input.name.toLowerCase().split(/\s+/).filter(t => t.length > 2);      const product = await ctx.prisma.product.create({        data: {          name: input.name,          slug,          description: input.description,          price: input.price,          mrp: input.mrp,          stock: input.stock,          unit: input.unit,          categoryId: input.categoryId,          images: imageUrls,          isFeatured: input.isFeatured ?? false,          gstRate: input.gstRate,          tags: autoTags,          lowStockThreshold: input.lowStockThreshold,        },      });      await logActivity({        type: 'PRODUCT',        action: 'CREATE',        entityId: product.id,        entityType: 'Product',        actorId: ctx.user.id,        actorName: 'Admin',        message: `Admin added a new product: ${product.name}`,        metadata: { name: product.name, price: product.price },      });      return product;    }),  update: adminProcedure    .input(      z.object({        id: z.string(),        name: z.string().min(3).optional(),        description: z.string().min(10).optional(),        price: z.number().positive().optional(),        mrp: z.number().positive().optional(),        stock: z.number().int().min(0).optional(),        unit: z.string().optional(),        categoryId: z.string().optional(),        isActive: z.boolean().optional(),        isFeatured: z.boolean().optional(),        gstRate: z.number().min(0).max(0.28).optional(),        tags: z.array(z.string()).optional(),        lowStockThreshold: z.number().int().min(1).optional(),      })    )    .mutation(async ({ ctx, input }) => {      const { id, ...updateData } = input;      const product = await ctx.prisma.product.findFirst({        where: { id },      });      if (!product) {        throw new TRPCError({ code: 'NOT_FOUND', message: 'Product not found or you do not own it.' });      }      const updated = await ctx.prisma.product.update({        where: { id },        data: updateData,      });      await logActivity({        type: 'PRODUCT',        action: 'UPDATE',        entityId: id,        entityType: 'Product',        actorId: ctx.user.id,        actorName: 'Admin',        message: `Admin updated product: ${updated.name}`,        metadata: updateData,      });      return updated;    }),  delete: adminProcedure    .input(z.object({ id: z.string() }))    .mutation(async ({ ctx, input }) => {      const product = await ctx.prisma.product.findFirst({        where: { id: input.id },        include: { _count: { select: { orderItems: true } } }      });            if (!product) {        throw new TRPCError({ code: 'NOT_FOUND', message: 'Product not found or you do not own it.' });      }      // Smart Delete: If product has been ordered, deactivate it instead of deleting      // to avoid breaking foreign key constraints and preserving history.      if (product._count.orderItems > 0) {        await ctx.prisma.product.update({          where: { id: input.id },          data: { isActive: false, isFeatured: false }        });        await logActivity({          type: 'PRODUCT',          action: 'UPDATE',          entityId: input.id,          entityType: 'Product',          actorId: ctx.user.id,          actorName: 'Admin',          message: `Admin deactivated product (sold previously): ${product.name}`,        });        return { success: true, message: 'Product has order history. It has been deactivated instead of deleted.' };      }      await ctx.prisma.product.delete({ where: { id: input.id } });      await logActivity({        type: 'PRODUCT',        action: 'DELETE',        entityId: input.id,        entityType: 'Product',        actorId: ctx.user.id,        actorName: 'Admin',        message: `Admin deleted product: ${product.name}`,      });      return { success: true, message: 'Product deleted successfully.' };    }),  addReview: protectedProcedure    .input(      z.object({        productId: z.string(),        rating: z.number().min(1).max(5),        comment: z.string().optional(),        images: z.array(z.string().startsWith('data:image/')).max(3).optional(),      })    )    .mutation(async ({ ctx, input }) => {      // Check if user has purchased this product      const hasPurchased = await ctx.prisma.order.findFirst({        where: {          userId: ctx.user.id,          status: 'DELIVERED',          items: { some: { productId: input.productId } },        },      });      if (!hasPurchased) {        throw new TRPCError({ code: 'FORBIDDEN', message: 'You can only review products you have purchased.' });      }      // M1: Prevent duplicate reviews — one review per user per product      const existingReview = await ctx.prisma.review.findUnique({        where: { productId_userId: { productId: input.productId, userId: ctx.user.id } },      });      if (existingReview) {        throw new TRPCError({ code: 'CONFLICT', message: 'You have already reviewed this product. Edit your existing review instead.' });      }      let reviewImages: string[] = [];      if (input.images) {        reviewImages = await Promise.all(          input.images.map(img => uploadImage(img, 'reviews'))        );      }      const newReview = await ctx.prisma.review.create({        data: {          productId: input.productId,          userId: ctx.user.id,          rating: input.rating,          comment: input.comment,          images: reviewImages,          isVerified: true, // Only buyers can review currently        },      });      // Recalculate product rating      const stats = await ctx.prisma.review.aggregate({        where: { productId: input.productId },        _avg: { rating: true },        _count: { id: true },      });      await ctx.prisma.product.update({        where: { id: input.productId },        data: {          rating: stats._avg.rating ?? 0,          reviewCount: stats._count.id,        },      });      return newReview;    }),  bulkUpload: adminProcedure    .input(      z.array(z.object({        name: z.string().min(3),        description: z.string().min(10),        price: z.number().positive(),        mrp: z.number().positive(),        stock: z.number().int().min(0),        unit: z.string(),        categoryName: z.string(),        images: z.array(z.string().url()).min(1).optional(), // URLs for bulk upload      }))    )    .mutation(async ({ ctx, input }) => {      const results: string[] = [];      const skipped: string[] = [];      const categories = await ctx.prisma.category.findMany();      for (const item of input) {        const category = categories.find(c => c.name.toLowerCase() === item.categoryName.toLowerCase());        if (!category) {          skipped.push(item.name); // track silently-dropped items          continue;        }        const slug = `${slugify(item.name, { lower: true, strict: true })}-${Date.now()}-${Math.random().toString(36).substring(7)}`;        const product = await ctx.prisma.product.create({          data: {            name: item.name,            slug,            description: item.description,            price: item.price,            mrp: item.mrp,            stock: item.stock,            unit: item.unit,            categoryId: category.id,            images: item.images ?? ['https://images.unsplash.com/photo-1542838132-92c53300491e?w=800'],            isActive: true,          },        });        results.push(product.id);      }      return {        success: true,        count: results.length,        skippedCount: skipped.length,        skippedNames: skipped, // caller can show which items need a valid category      };    }),});