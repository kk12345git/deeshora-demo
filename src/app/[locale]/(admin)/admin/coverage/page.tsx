'use client';import { useState } from 'react';import { trpc } from '@/lib/trpc';import {  MapPin, Plus, Trash2, CheckCircle, Clock, Edit3, Save, X,  Loader2, ToggleLeft, ToggleRight, EyeOff, Eye, AlertTriangle,  Globe, ShieldCheck, Layers, ChevronRight, Navigation} from 'lucide-react';import type { ServiceArea } from '@prisma/client';import dynamic from 'next/dynamic';import { motion, AnimatePresence } from 'framer-motion';const CoverageMap = dynamic(() => import('@/components/admin/CoverageMap'), {  ssr: false,  loading: () => (    <div className="h-[400px] w-full bg-gray-50 rounded-3xl flex items-center justify-center border-2 border-dashed border-gray-200">      <div className="flex flex-col items-center gap-3">        <Loader2 className="w-10 h-10 animate-spin text-brand-500" />        <p className="text-[10px] font-black text-gray-400 uppercase tracking-widest">Initalizing Geospatial Node...</p>      </div>    </div>  )});type EditingArea = Partial<ServiceArea> & { isNew?: boolean };export default function CoveragePage() {  const utils = trpc.useUtils();  const { data: areas = [], isLoading } = trpc.admin.getAllServiceAreas.useQuery();  const [editing, setEditing] = useState<EditingArea | null>(null);  const [deleteConfirm, setDeleteConfirm] = useState<string | null>(null);  const createArea = trpc.admin.createServiceArea.useMutation({    onSuccess: () => { utils.admin.getAllServiceAreas.invalidate(); utils.admin.getServiceAreas.invalidate(); setEditing(null); },  });  const updateArea = trpc.admin.updateServiceArea.useMutation({    onSuccess: () => { utils.admin.getAllServiceAreas.invalidate(); utils.admin.getServiceAreas.invalidate(); setEditing(null); },  });  const deleteArea = trpc.admin.deleteServiceArea.useMutation({    onSuccess: () => { utils.admin.getAllServiceAreas.invalidate(); utils.admin.getServiceAreas.invalidate(); setDeleteConfirm(null); },  });  const toggleServiceable = (area: ServiceArea) =>    updateArea.mutate({ id: area.id, isServiceable: !area.isServiceable });  const toggleActive = (area: ServiceArea) =>    updateArea.mutate({ id: area.id, isActive: !area.isActive });  const liveCount   = areas.filter(a => a.isServiceable && a.isActive).length;  const soonCount   = areas.filter(a => !a.isServiceable && a.isActive).length;  const hiddenCount = areas.filter(a => !a.isActive).length;  const startNew = () =>    setEditing({ isNew: true, label: '', value: '', zone: 'Thiruvottriyur', pincode: '', isServiceable: true, sortOrder: 0 });  const handleSave = () => {    if (!editing) return;    if (editing.isNew) {      createArea.mutate({        label: editing.label ?? '',        value: editing.value ?? '',        zone: editing.zone ?? 'Thiruvottriyur',        pincode: editing.pincode ?? undefined,        isServiceable: editing.isServiceable ?? true,        sortOrder: editing.sortOrder ?? 0,        coordinates: editing.coordinates,      });    } else {      updateArea.mutate({        id: editing.id!,        label: editing.label,        zone: editing.zone,        pincode: editing.pincode ?? undefined,        isServiceable: editing.isServiceable,        sortOrder: editing.sortOrder,        coordinates: editing.coordinates,      });    }  };  const isSaving = createArea.isPending || updateArea.isPending;  const zones = Array.from(new Set(areas.map(a => a.zone)));  return (    <div className="space-y-12">      {/* Header */}      <div className="flex flex-col xl:flex-row xl:items-end justify-between gap-8">        <motion.div           initial={{ opacity: 0, x: -20 }}          animate={{ opacity: 1, x: 0 }}        >          <div className="flex items-center gap-2 mb-2">             <div className="w-2 h-2 rounded-full bg-brand-500 shadow-[0_0_10px_rgba(249,115,22,0.4)]" />             <p className="text-[10px] font-black text-gray-400 uppercase tracking-[0.3em]">Logistics Grid</p>          </div>          <h1 className="text-5xl font-black text-gray-900 tracking-tight leading-none uppercase">Coverage Areas</h1>          <p className="text-gray-400 font-bold mt-3 text-sm flex items-center gap-2">            <Globe size={14} className="text-emerald-500" />            Managing <span className="text-gray-900 font-black">geospatial availability</span> and delivery locality nodes.          </p>        </motion.div>        <motion.button          initial={{ opacity: 0, scale: 0.95 }}          animate={{ opacity: 1, scale: 1 }}          onClick={startNew}          className="h-16 px-8 bg-gray-950 text-white rounded-[1.5rem] font-black text-[10px] uppercase tracking-[0.2em] flex items-center justify-center gap-3 transition-all shadow-2xl active:scale-95"        >          <Plus size={16} /> Deploy New Node        </motion.button>      </div>      {/* Stats Row */}      <div className="grid grid-cols-1 md:grid-cols-3 gap-8">        {[          { label: 'Operational Nodes', value: liveCount,   color: 'emerald', icon: <CheckCircle size={22} />, desc: 'Live fulfillment active' },          { label: 'Pending Deployment', value: soonCount,   color: 'brand',  icon: <Clock size={22} />, desc: 'Expansion in progress' },          { label: 'Restricted Zones', value: hiddenCount, color: 'gray',    icon: <EyeOff size={22} />, desc: 'Access disabled' },        ].map(({ label, value, color, icon, desc }, i) => (          <motion.div             key={label}            initial={{ opacity: 0, y: 20 }}            animate={{ opacity: 1, y: 0 }}            transition={{ delay: i * 0.1 }}            className="bg-white p-8 rounded-[3rem] border border-gray-100 shadow-sm flex items-center gap-6 group hover:shadow-xl hover:shadow-gray-100/50 transition-all cursor-default"          >            <div className={`w-14 h-14 bg-${color}-50 rounded-2xl flex items-center justify-center text-${color}-600 group-hover:scale-110 transition-transform`}>{icon}</div>            <div>              <p className="text-3xl font-black text-gray-900 tracking-tighter">{value}</p>              <p className="text-[10px] font-black text-gray-400 uppercase tracking-widest mt-0.5">{label}</p>              <p className={`text-[8px] font-black text-${color}-500/50 uppercase tracking-[0.2em] mt-1`}>{desc}</p>            </div>          </motion.div>        ))}      </div>      {/* Map Overview */}      <motion.div         initial={{ opacity: 0, scale: 0.98 }}        animate={{ opacity: 1, scale: 1 }}        className="space-y-4"      >        <div className="flex items-center justify-between px-2">          <h2 className="text-[10px] font-black text-gray-900 uppercase tracking-[0.4em] flex items-center gap-3">            <Navigation size={16} className="text-brand-500" />            Global Positional Audit          </h2>          <div className="flex items-center gap-2 text-[8px] font-black text-gray-400 uppercase tracking-widest">             <div className="w-2 h-2 rounded-full bg-emerald-500 animate-pulse" />             Interactive Radar Active          </div>        </div>        <div className="p-2 bg-white rounded-[4rem] border border-gray-100 shadow-2xl shadow-gray-200/40">           <CoverageMap areas={areas} />        </div>      </motion.div>      {/* Add / Edit Form */}      <AnimatePresence>        {editing && (          <motion.div             initial={{ opacity: 0, height: 0 }}            animate={{ opacity: 1, height: 'auto' }}            exit={{ opacity: 0, height: 0 }}            className="overflow-hidden"          >            <div className="bg-gray-950 rounded-[4rem] p-10 space-y-10 shadow-2xl relative">              <div className="absolute top-0 right-0 p-10 opacity-5 pointer-events-none">                 <ShieldCheck size={160} className="text-brand-500" />              </div>              <div className="flex items-center justify-between relative z-10">                <div>                  <h2 className="text-2xl font-black text-white tracking-tight uppercase">                    {editing.isNew ? 'Initialize Locality Node' : `Audit Terminal: ${editing.label}`}                  </h2>                  <p className="text-[10px] font-black text-gray-500 uppercase tracking-[0.2em] mt-1">Configuring Geospatial parameters</p>                </div>                <button onClick={() => setEditing(null)} className="w-12 h-12 bg-white/5 hover:bg-white/10 rounded-2xl flex items-center justify-center text-white transition-colors">                  <X size={20} />                </button>              </div>              <div className="grid grid-cols-1 xl:grid-cols-2 gap-10 relative z-10">                <div className="space-y-8">                  <div className="grid grid-cols-1 sm:grid-cols-2 gap-8">                    <Field label="Identity Label" dark>                      <input                        value={editing.label ?? ''}                        onChange={e => setEditing(p => ({ ...p, label: e.target.value }))}                        className="w-full h-16 px-6 bg-white/5 border-2 border-transparent rounded-2xl font-black text-white focus:border-brand-500 outline-none transition-all placeholder:text-gray-700 uppercase"                        placeholder="e.g. Kathivakkam"                      />                    </Field>                    <Field label="System Value (Permanent)" dark>                      <input                        value={editing.value ?? ''}                        onChange={e => setEditing(p => ({ ...p, value: e.target.value }))}                        disabled={!editing.isNew}                        className="w-full h-16 px-6 bg-white/5 border-2 border-transparent rounded-2xl font-mono text-xs font-black text-emerald-400 focus:border-emerald-500 outline-none transition-all placeholder:text-gray-700 uppercase disabled:opacity-20"                        placeholder="e.g. kathivakkam_node"                      />                    </Field>                    <Field label="Zone Partition" dark>                      <input                        value={editing.zone ?? ''}                        onChange={e => setEditing(p => ({ ...p, zone: e.target.value }))}                        className="w-full h-16 px-6 bg-white/5 border-2 border-transparent rounded-2xl font-black text-white focus:border-brand-500 outline-none transition-all placeholder:text-gray-700 uppercase"                        placeholder="e.g. North Chennai"                      />                    </Field>                    <Field label="Network Pincode" dark>                      <input                        value={editing.pincode ?? ''}                        onChange={e => setEditing(p => ({ ...p, pincode: e.target.value.replace(/\D/g, '').slice(0, 6) }))}                        className="w-full h-16 px-6 bg-white/5 border-2 border-transparent rounded-2xl font-black text-white focus:border-brand-500 outline-none transition-all placeholder:text-gray-700"                        placeholder="600019"                      />                    </Field>                  </div>                  <div className="grid grid-cols-1 sm:grid-cols-2 gap-8">                    <Field label="Priority sequence" dark>                      <input                        type="number"                        value={editing.sortOrder ?? 0}                        onChange={e => setEditing(p => ({ ...p, sortOrder: parseInt(e.target.value) || 0 }))}                        className="w-full h-16 px-6 bg-white/5 border-2 border-transparent rounded-2xl font-black text-white focus:border-brand-500 outline-none transition-all"                      />                    </Field>                    <Field label="Deployment State" dark>                      <div className="flex gap-3 h-16">                        {[true, false].map(val => (                          <button                            key={String(val)}                            onClick={() => setEditing(p => ({ ...p, isServiceable: val }))}                            className={`flex-1 rounded-2xl font-black text-[10px] uppercase tracking-widest transition-all ${                              editing.isServiceable === val                                ? val ? 'bg-emerald-500 text-white shadow-lg shadow-emerald-500/20' : 'bg-brand-500 text-white shadow-lg shadow-brand-500/20'                                : 'bg-white/5 text-gray-500 hover:bg-white/10'                            }`}                          >                            {val ? 'Live Node' : 'Coming Soon'}                          </button>                        ))}                      </div>                    </Field>                  </div>                  <div className="flex gap-4 pt-4">                    <button                      onClick={handleSave}                      disabled={isSaving || !editing.label || !editing.value}                      className="flex-1 h-20 bg-brand-600 hover:bg-brand-700 text-white rounded-[1.75rem] font-black text-xs uppercase tracking-[0.2em] flex items-center justify-center gap-3 transition-all shadow-2xl active:scale-95 disabled:opacity-20"                    >                      {isSaving ? <Loader2 size={20} className="animate-spin" /> : <Save size={20} />}                      {editing.isNew ? 'Deploy Locality' : 'Commit Audit'}                    </button>                    <button onClick={() => setEditing(null)} className="px-10 h-20 rounded-[1.75rem] bg-white/5 text-white font-black text-xs uppercase tracking-widest hover:bg-white/10 transition-colors">                      Abort                    </button>                  </div>                </div>                <div className="space-y-6">                  <div className="flex items-center justify-between px-2">                     <p className="text-[10px] font-black text-gray-500 uppercase tracking-[0.2em]">Positional Terminal</p>                     <div className="flex gap-4">                        <div className="text-[10px] font-mono text-emerald-400/60">                           LAT: {(editing.coordinates as any)?.lat?.toFixed(6) || 'NULL'}                        </div>                        <div className="text-[10px] font-mono text-emerald-400/60">                           LNG: {(editing.coordinates as any)?.lng?.toFixed(6) || 'NULL'}                        </div>                     </div>                  </div>                  <div className="p-2 bg-white/5 rounded-[3rem] border border-white/5">                     <CoverageMap                        center={editing.coordinates ? [(editing.coordinates as any).lat, (editing.coordinates as any).lng] : undefined}                       onLocationSelect={(lat, lng) => setEditing(p => ({ ...p, coordinates: { lat, lng } }))}                      />                  </div>                  <div className="flex items-center gap-3 text-brand-500/40 px-6 py-4 bg-brand-500/5 rounded-2xl border border-brand-500/10">                     <AlertTriangle size={18} />                     <p className="text-[9px] font-black uppercase tracking-[0.2em] leading-relaxed">System warning: updating positional coordinates will immediately affect vendor discovery radar for this locality.</p>                  </div>                </div>              </div>            </div>          </motion.div>        )}      </AnimatePresence>      {/* Areas Table by Zone */}      {isLoading ? (        <div className="flex flex-col items-center justify-center py-40 gap-4">           <Loader2 className="w-12 h-12 animate-spin text-brand-500" />           <p className="text-[10px] font-black text-gray-400 uppercase tracking-[0.4em]">Deciphering Zone partitions...</p>        </div>      ) : (        <div className="space-y-10">          {zones.map((zone, zi) => {            const zoneAreas = areas.filter(a => a.zone === zone);            return (              <motion.div                 key={zone}                 initial={{ opacity: 0, y: 20 }}                animate={{ opacity: 1, y: 0 }}                transition={{ delay: zi * 0.1 }}                className="bg-white rounded-[3.5rem] border border-gray-100 shadow-xl shadow-gray-200/20 overflow-hidden"              >                {/* Zone Header */}                <div className="flex items-center gap-4 px-10 py-8 border-b border-gray-50 bg-gray-50/30">                  <div className="w-10 h-10 bg-white rounded-xl flex items-center justify-center shadow-sm border border-gray-100">                    <Layers size={18} className="text-brand-500" />                  </div>                  <div>                    <h3 className="font-black text-gray-900 text-lg uppercase tracking-tight">{zone}</h3>                    <p className="text-[9px] font-black text-gray-400 uppercase tracking-widest mt-0.5">{zoneAreas.length} LOCALITIES INTEGRATED</p>                  </div>                </div>                {/* Table */}                <div className="divide-y divide-gray-50">                  {zoneAreas.map(area => (                    <div key={area.id} className={`flex items-center gap-8 px-10 py-8 transition-all group ${!area.isActive ? 'opacity-40 bg-gray-50/50' : 'hover:bg-gray-50/80'}`}>                      {/* Status pill */}                      <div className="w-24 flex-shrink-0">                        <AnimatePresence mode="wait">                          {!area.isActive ? (                            <motion.span key="hidden" initial={{ opacity: 0 }} animate={{ opacity: 1 }} className="inline-flex items-center gap-1.5 text-[9px] font-black uppercase tracking-widest text-gray-400 bg-gray-100 px-3 py-1.5 rounded-full border border-gray-200">                              <EyeOff size={10} /> HIDE                            </motion.span>                          ) : area.isServiceable ? (                            <motion.span key="live" initial={{ opacity: 0 }} animate={{ opacity: 1 }} className="inline-flex items-center gap-1.5 text-[9px] font-black uppercase tracking-widest text-emerald-600 bg-emerald-50 px-3 py-1.5 rounded-full border border-emerald-100">                              <CheckCircle size={10} /> LIVE                            </motion.span>                          ) : (                            <motion.span key="soon" initial={{ opacity: 0 }} animate={{ opacity: 1 }} className="inline-flex items-center gap-1.5 text-[9px] font-black uppercase tracking-widest text-brand-600 bg-brand-50 px-3 py-1.5 rounded-full border border-brand-100">                              <Clock size={10} /> SOON                            </motion.span>                          )}                        </AnimatePresence>                      </div>                      {/* Label + meta */}                      <div className="flex-1 min-w-0">                        <p className="font-black text-gray-950 text-base uppercase tracking-tight group-hover:text-brand-600 transition-colors">{area.label}</p>                        <div className="flex items-center gap-3 mt-1.5">                           <code className="text-[10px] font-mono font-black text-gray-300 bg-gray-50 px-2 py-0.5 rounded border border-gray-100 uppercase tracking-tighter">IDX_{area.value}</code>                           <span className="w-1 h-1 rounded-full bg-gray-200" />                           <p className="text-[10px] font-black text-gray-400 uppercase tracking-widest">                             {area.pincode || 'NO_ZIP'} · SEQ_{area.sortOrder}                           </p>                        </div>                      </div>                      {/* Action buttons */}                      <div className="flex items-center gap-3 flex-shrink-0 opacity-0 group-hover:opacity-100 transition-all translate-x-4 group-hover:translate-x-0">                        <button                          onClick={() => toggleServiceable(area)}                          className={`w-12 h-12 rounded-2xl flex items-center justify-center transition-all ${                            area.isServiceable                              ? 'bg-emerald-50 text-emerald-600 hover:bg-emerald-600 hover:text-white'                              : 'bg-brand-50 text-brand-600 hover:bg-brand-600 hover:text-white'                          }`}                        >                          {area.isServiceable ? <ToggleRight size={20} /> : <ToggleLeft size={20} />}                        </button>                        <button                          onClick={() => toggleActive(area)}                          className="w-12 h-12 rounded-2xl bg-gray-100 text-gray-400 hover:bg-gray-950 hover:text-white flex items-center justify-center transition-all"                        >                          {area.isActive ? <Eye size={18} /> : <EyeOff size={18} />}                        </button>                        <button                          onClick={() => setEditing({ ...area })}                          className="w-12 h-12 rounded-2xl bg-gray-100 text-gray-400 hover:bg-brand-600 hover:text-white flex items-center justify-center transition-all"                        >                          <Edit3 size={18} />                        </button>                        {deleteConfirm === area.id ? (                          <div className="flex items-center gap-2 bg-red-50 p-1.5 rounded-2xl border border-red-100">                            <button                              onClick={() => deleteArea.mutate({ id: area.id })}                              disabled={deleteArea.isPending}                              className="text-[9px] font-black text-white bg-red-600 hover:bg-red-700 px-4 py-2 rounded-xl transition-all uppercase tracking-widest shadow-lg shadow-red-600/20"                            >                              Confirm                            </button>                            <button onClick={() => setDeleteConfirm(null)} className="p-2 text-gray-400 hover:text-gray-900"><X size={16} /></button>                          </div>                        ) : (                          <button                            onClick={() => setDeleteConfirm(area.id)}                            className="w-12 h-12 rounded-2xl bg-gray-100 text-gray-400 hover:bg-red-600 hover:text-white flex items-center justify-center transition-all"                          >                            <Trash2 size={18} />                          </button>                        )}                      </div>                                            <ChevronRight size={18} className="text-gray-100 group-hover:text-brand-500 transition-colors ml-4" />                    </div>                  ))}                </div>              </motion.div>            );          })}        </div>      )}      {/* Info box */}      <motion.div         initial={{ opacity: 0 }}        animate={{ opacity: 1 }}        className="bg-gray-950 rounded-[3rem] p-10 flex flex-col md:flex-row items-start gap-8 relative overflow-hidden"      >        <div className="absolute top-0 left-0 w-full h-full bg-gradient-to-br from-blue-500/10 to-transparent pointer-events-none" />        <div className="w-16 h-16 bg-white/5 rounded-2xl flex items-center justify-center text-blue-400 shrink-0 border border-white/5">           <AlertTriangle size={32} />        </div>        <div className="space-y-6 relative z-10">          <div>            <h4 className="text-lg font-black text-white uppercase tracking-tight">Geospatial Rulebook</h4>            <p className="text-gray-500 text-sm mt-1 uppercase tracking-widest font-black">Fulfillment Logic Hierarchy</p>          </div>          <div className="grid grid-cols-1 md:grid-cols-3 gap-8">             <InfoItem label="Operational Node" desc="Indicates active fulfillment capability. Visible to all customers in selection matrices." color="text-emerald-400" />             <InfoItem label="Latency Node" desc="Expansion target. Visible as 'Coming Soon' to build regional anticipation." color="text-brand-400" />             <InfoItem label="Stealth Node" desc="Internal testing or deprecated zones. Completely invisible to end-users." color="text-gray-500" />          </div>        </div>      </motion.div>    </div>  );}function InfoItem({ label, desc, color }: { label: string, desc: string, color: string }) {  return (    <div className="space-y-2">       <p className={`text-[10px] font-black uppercase tracking-[0.2em] ${color}`}>{label}</p>       <p className="text-xs text-gray-400 leading-relaxed font-medium">{desc}</p>    </div>  );}function Field({ label, children, dark }: { label: string; children: React.ReactNode; dark?: boolean }) {  return (    <div className="space-y-3">      <label className={`text-[9px] font-black uppercase tracking-[0.3em] ${dark ? 'text-gray-500' : 'text-gray-400'}`}>{label}</label>      {children}    </div>  );}